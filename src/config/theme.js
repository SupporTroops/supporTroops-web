import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#023E8A",
        },
        secondary: {
            main: "#00B4DB",
        },
        grey: {
            400: "#ffffff7d",
            300: "#ffffffdb",
        },
    },
});

export default theme;
