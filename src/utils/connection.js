const connection = {
    login: (loginDetails) => {
        // fetchAPI
        return new Promise((resolve, reject) => {
            resolve(true);
        });
    },
    createAccount: (signupDetails) => {
        // fetchAPI
        return new Promise((resolve, reject) => {
            resolve(true);
        });
    },
};

export default connection;
